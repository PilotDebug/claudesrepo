# Hello, C++

The starter every new C++ lab is copied from. Built with plain `make` and `g++ -std=c++20`: `make run`, `make test`. Every `.cpp` other than `main.cpp` and `test_*.cpp` is linked into both.
